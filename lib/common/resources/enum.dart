enum Environment{dev, prod}
enum DateTimeFormatForApiResponse{utc, nonUtc}
// A real number is a number that can be positive, negative, or zero, and have a decimal part.
// An integer number is a number that can be positive, negative, or zero, but has no decimal part.
enum TextFieldVariant {email, password, user, mobileNumber, realNumber, integerNumber, address, date, fullName, firstName, lastName, item}
enum ButtonVariant {btnPrimary, btnAccent, btnDisable, btnWithBorder }
enum ButtonSize {large, medium, small}
enum ToastState{success, fail, info}
enum MultimediaFileType{pdfOnly,imageOnly, docOnly, videoOnly, allTypes}
enum VideoSizeType{largeSingleVideoFrame, smallChatVideoFrame}
enum LanguageCode{en, de}
enum Legals{ imprint, termsConditions, privacyPolicy}
enum OS{android, ios}
enum ChatType {video, image, pdfDoc, text, audio}


